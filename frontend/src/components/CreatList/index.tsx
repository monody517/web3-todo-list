import { Button } from "@web3uikit/core";

const CreatList = () => {
  return (
    <>
      <Button
        color="red"
        onClick={function noRefCheck() {}}
        size="large"
        text="creat event"
        theme="moneyPrimary"
        type="button"
      />
    </>
  );
};

export default CreatList;
